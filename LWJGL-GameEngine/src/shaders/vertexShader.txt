//Versão do glsl
#version 400 core

//INPUT (3D VECTOR) posição do vertice (ta no vao)

in vec3 position;
in vec2 textureCoords;

out vec2 pass_textureCoords;

void main(void){
	
	gl_Position = vec4(position, 1.0);
	pass_textureCoords = textureCoords;
}